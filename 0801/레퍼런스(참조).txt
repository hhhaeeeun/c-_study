포인터 변수는 4바이트를 할당하여 주소값을 저장하는 새로운 메모리를 생성하여 간접참조를 수행한다. 
레퍼런스 변수는 할당 없이 동일한 메모리 공간을 참조하며, 선언과 동시에 초기화 되고, 변경이 불가능하다. (별칭의 개념)

매개변수로 포인터 전달 시 메모리 소모가 일어나고 값이 복사된다.
매개변수로 레퍼런스 전달 시 메모리 소모가 없고 값 복사도 발생하지 않는다.(애초에 선언된 그 자체를 참조)